# NAS-AI Redesign Tracker

## Ground Rules
- One step at a time. Discuss → agree → implement.
- No hardcoding in code or config files.
- Every change is covered by a smoke test before and after.
- GitHub commit after each completed step.
- Breaking something while fixing something else = rollback immediately.

---

## System State (current — 2026-07-05)

| Component | Status |
|---|---|
| LM Studio (Mac M1 Pro) | Running — serves embeddings + LLM |
| Embeddings model | `text-embedding-bge-large-en-v1.5` (1024 dims) |
| LLM model | Qwen 2.5 14B (upgraded from meta-llama-3.1-8b-instruct) |
| ai-pgvector (NAS) | Running at port 5433, db=nasai — PRIMARY STORE |
| UI framework | Migrated from Streamlit (`ui_app.py`) to NiceGUI (`ui/app.py` + panel modules: `ask.py`, `chat.py`, `collections_view.py`, `knowledge_graph.py`, `system_config.py`, `validation.py`, `data_prep.py`, `preview.py`, `sql_inspector.py`, `ingestion.py`, `analysis.py`, `debug.py`, `render.py`) |
| FIX collection (`xml_test`) | 13/13 smoke tests passing |
| BBG collection (`bbg_fields`) | Test subset (730/69k rows) — full ingest pending |
| KB docs (`kb_docs`) | 348 chunks (178 active articles, inactive filtered) — 10/10 smoke tests passing |
| Obsidian (`obsidian`) | 729 mixed + 37 narrative + 62 procedural + 71 reference chunks — smoke tests passing |
| recon_assist_file | 89 chunks — 10/10 smoke tests passing |
| astro_catalog | 13,336 chunks (OpenNGC + Messier merged via Data Prep) |
| astro_test | 3 files — ASTRO-001 parser fix verified (M42 target correct) |
| M51 | 505 chunks — schema-driven astro roles (ASTRO-004), clean schema after coalescing |
| Metadata SQL path | **Live** — `core/metadata_query.py` handles count/list/group-by intents via parameterized SQL (Phase 4.6) |
| Structured query planner | **Removed entirely** — was always dry-run, added latency with zero benefit |
| BGE reranker | Disabled for `entity_row` doc_type — was demoting correct KB results when given 25 candidates |

---

## Known Issues

| ID | Area | Description | Status |
|---|---|---|---|
| RET-010 | Structured planner | Ranking balance for phrases like "executing broker" in long descriptions. | Resolved — planner removed entirely (see CODE-030) |
| CODE-005 | xml_serializer.py | Function-level state (`_all_rows`, `_files_seen`) for cross-file buffering — corrupts silently on crash. | Partially resolved — buffer now reset unconditionally after full finalize (stale state was triggering a FULL finalize per file on re-ingest: re-embedding + duplicate upserts); schema failure of one file no longer aborts the batch. Function-attr design itself still pending replacement |
| CODE-010 | ingestion | Collection name mismatch between UI and PostgreSQL collections table causes ForeignKeyViolation. | Pending |
| CODE-012 | ui | Ask/Chat tabs need a "Clear cache" button | Pending |
| CODE-013 | ui | BM25/lexical candidates not shown in debug panel | Pending |
| CODE-014 | ui | System Config tab shows embeddings model but not LLM model — add LLM model + base_url + timeout fields | Pending |
| CODE-015 | db schema | files table missing last_ingested column | Pending |
| CODE-020 | concept_vector_builder.py | Single-file/entity_row collections grouped by source_file — useless labels | **Resolved** — CV-01/02/03/04 (see below) |
| CODE-021 | related_titles_linker.py | Obsidian wikilinks not persisted to cross_links table | **Resolved** — 43 wikilinks confirmed |
| CODE-022 | ui / knowledge_graph.py | No Knowledge Graph UI | **Resolved** — full tab built: graph, concept clusters, cross-link review (grouped by target), build, danger zone |
| CODE-023 | chat_engine.py | Multi-item queries ("tags 453, 452, and 448") only retrieve one item | **Resolved** — deterministic gate (≥2 identifier tokens + separator), LLM splitter (grounded validation, cap 5), parallel fan-out via run_parallel_queries(single_item=True), per-item merged answer. smoke_multi_item.py 18/18 |
| CODE-024 | recon_assist_file | NULL primary_name/identifier rows from merged Excel cells. Concrete impact: `primary_name contains Goldman` returns 15 not 16 (one Goldman row has NULL job name) | Pending — Data Prep Excel upload + forward-fill (CODE-025) is the fix path |
| CODE-025 | ui_data_prep.py | Data Prep tab only accepts CSV — add Excel (.xlsx) upload with sheet selector + forward-fill | Pending |
| CODE-026 | ui_data_prep.py | Add PostgreSQL chunk editor — load collection chunks, edit payload fields inline, save with confirmation | Pending |
| CODE-027 | chat_engine.py | Tier 1 identifier match in `select_collections` overrode Tier 2 LLM ordering entirely, ignoring procedural intent | **Resolved** — Tier 1 hits now merged AFTER Tier 2 ordering, not returned early |
| CODE-028 | chat_engine.py | `augment_query_with_focus` injected stale history terms into self-contained follow-up questions | **Resolved** — skips injection when question has its own filename/tag identifiers |
| CODE-029 | chat_engine.py | Hallucination when `primary_answer` = "No answer found" — LLM fabricated plausible-looking content | **Resolved** — skip LLM entirely, return clean "couldn't find" message |
| CODE-030 | structured_query_planner.py, structured_plan_executor.py, router.py, ui/system_config.py, chat_engine.py | Structured query planner always ran in dry-run mode (never executed), added LLM call latency with zero functional benefit | **Resolved** — both files deleted, all references removed from router.py, ui/system_config.py, chat_engine.py |
| CODE-031 | system.json | BGE reranker (`apply_to_doc_types` included `entity_row`) demoted correct top-RRF-ranked KB article when given 25 candidates (e.g. promoted `1.0 - RECON Mappings` over `2.3 Logging in to sFTP` despite RRF score 10x lower) | **Resolved** — removed `entity_row` from `bge_reranker.apply_to_doc_types`; RRF ranking alone is more reliable for kb_docs |
| DISPLAY-001 | ui | Two-tier table header not supported by st.dataframe — needs HTML render or AgGrid | Deferred |
| DISCOVERY-001 | discovery.py | discovery_count returned retrieval-capped counts (top-k limited: 4 instead of 178, 41 instead of 947) | **Resolved** — Phase 4.6 metadata SQL path answers discovery_count/discovery_list via direct parameterized SQL; discovery engine remains as fallback |
| OBS-001 | doc_serializer.py | Obsidian over-chunking on large notes (44KB → 246 chunks) | Low priority |
| ASTRO-001 | astro_parser.py, config/astro_metadata.json | Filename parser misread target when frame-type prefix absent (M42_2.0s → target=2.0s) | **Resolved** — positional `offset` shift in `_apply_filename_pattern` when position-0 allowed_values don't match, + `"default": "light"` on file_frame_type (no frame token = light frame) |
| ASTRO-002 | astro_parser.py | XISF files have image geometry only — no astrometric metadata | Low priority |
| ASTRO-003 | astro_serializer.py | primary_name for astro files without FITS OBJECT keyword falls back to filename-parsed target (see ASTRO-001) | **Resolved** — schema-driven primary_name with `file_target` appended as final fallback (ASTRO-004) |
| ASTRO-004 | ASTRO/schema_inference_astro.py (new), astro_serializer.py | Astro roles were hardcoded in serializer (no schema step like tables) | **Resolved** — same flow as tables: existing schema → LLM inference → heuristic fallback; collection-level schema (source_file_stem = collection_name, one LLM call per collection); serializer resolves primary_name via schema role fields |
| ASTRO-005 | astro_parser.py | Duplicate metadata concepts (`gain`/`file_gain`, `object`/`target`/`file_target`) caused unstable filter/field picks in metadata queries | **Resolved** — `_merge_filename_metadata` generic fill: every `file_X` fills empty canonical `X`; `file_*` kept in payload for traceability, excluded from schema inference input. Retest AG-05/09 after M51 re-ingest |
| ASTRO-006 | collections.json | Astro collections had empty/thin routing_description — image/gain/rotation questions routed to obsidian | Partially resolved — descriptions added for astro_test/M51; extend with rotation/angle/date terms |
| MQ-01 | metadata_query.py | LLM spec extraction unstable — filters/fields vary run-to-run (Qwen 14B); four different answers observed for same question before mitigations | Open — mitigations in place (deterministic regrounding, zero-result repair, schema-aware override, value grounding) but root LLM variance remains |
| MQ-02 | chat_engine.py | Chat fan-out + phrasing drift yields different metadata answers for same question across collections/runs | Open |
| SCHEMA-01 | core/schema_inference.py | LLM schema inference misclassified filename/script columns and entity-name columns | **Resolved** — deterministic pre-LLM pass: columns with ≥70% values matching `name.ext` pattern → reference_identifier (excluded from LLM prompt); prompt: entity/org names → type, comments → description, numeric measurements + dates → other |
| DB-001 | PostgreSQL trigger | Slash-delimited category paths tokenize as compound token | Resolved |
| DB-002 | nlp_generator.py | type_value not included in build_structured_nlp_text | Resolved |

**Retired issues (fully resolved, no longer tracked in detail):**
RET-011, CODE-001–004, CODE-007–009, CODE-016–019, DISCOVERY-002, STAGE0-001, PENDING-001 (superseded by CL-03/CL-05 below)

**Key learnings (debugging):**
- psycopg2 `%` escaping in ILIKE needs `%%`.
- Always define known-correct ground truth before testing.
- Smoke test hardcoded values are fragile — use "match"/"0 match" pattern.
- tsvector tokenization: preprocess slashes to spaces before building.
- nlp_text completeness: if a payload field isn't in nlp_text, BM25 can't find it. **Verified this session: `nlp_text` is a dedicated PostgreSQL column (full chunk text), separate from `payload->>'nlp_text'` which is always empty — do not confuse the two when debugging BM25.**
- XISF vs FITS: FITS has rich astrometric headers, XISF (PixInsight output) typically doesn't.
- Astro filename conventions vary — prioritize FITS header fields over filename parsing.
- Catalog joins: normalize before fuzzy matching (strip spaces, lowercase, add prefix).
- LLM max_tokens: increase to 2048 for grounded responses with multi-source context.
- Faithfulness guard scope: must check against `primary_answer` only, not full `retrieved` string.
- OCR inline in text field causes garbage in chat previews — store only in `embedded_image_ocr_map`.
- **Reranker candidate-set size matters**: BGE reranker gave correct ranking with 10 candidates but wrong ranking with 25 candidates for the same query — always test rerankers at the actual production `top_k`, not a smaller sample.
- **`doc_type` is a weak proxy for "how should I group this"** — two collections can share `doc_type=entity_row` (kb_docs, astro_catalog) but need completely different grouping strategies. Route on the *actual presence* of a usable field (tags %, type diversity, folder depth) rather than doc_type category.
- **Local 14B-class LLMs are unreliable at semantic label deduplication** — even with an explicit guarded prompt (environment/stage warnings, "when unsure don't merge"), Qwen 2.5 14B produced merges like `QA Archive → PROD Archive` and `Repo Collateral → Recon Missing Files`. This class of judgment task needs either a larger model or a different (embedding-similarity-gated) approach — revisit in Phase 5.
- **Mention-based cross-linking was removed, not just tuned** — broker/client names (Goldman, Moore, CITI) are too short and too common to serve as reliable mention-match anchors across collections. The current discoverer only does exact-identifier and name-similarity matching; true cross-collection linking for free-text mentions needs NER (CL-03), not string matching.
- **Raw-value embeddings pick the wrong field; concept-vector labels pick the right one** — embedding the question against a field's raw values fails when another field's values lexically resemble the question (job names contain "RECON"/"PB" and beat broker names for "which broker..."). Embedding against concept vector *group labels* works because the labels are already concept-level. Concept vectors now serve double duty: routing topics AND metadata-query field selection.
- **Every nondeterministic answer traced to an unordered iteration or an unguarded LLM pick** — the 4-different-answers bug was dict iteration order in filter regrounding. When an LLM output feeds a choice, either ground it (real value lists), guard it (schema-generic-only override), or repair it (zero-result filter drop) — and make every tie-break deterministic (sorted()).
- **Coalesce duplicate metadata at ingest, not at query time** — two fields for the same concept (`gain`/`file_gain`, `object`/`file_target`) made every downstream query a coin-flip. Generic fill rule (file_X fills empty X) removed the ambiguity without losing traceability.
- **Prompt tuning has a ceiling; deterministic pre-passes don't** — 3 prompt attempts failed to stop the LLM classifying a filename column as `type`; a 5-line regex pre-pass (≥70% values match `name.ext` → reference_identifier) fixed it permanently and generically.
- **Wikilinks ≠ cross-collection links** — obsidian's `[[wikilinks]]` only capture note-to-note relationships inside the same collection. They do not and cannot bridge from a structured collection (recon_assist_file) into obsidian; that gap is only closed by NER-based tagging (CL-03/CL-05) or a confirmed identifier match already present in cross_links.

---

## Roadmap

### Phase 1 — Stabilize FIX retrieval ✅ COMPLETE

### Phase 1b — FIX version separation
| Step | Description | Status |
|---|---|---|
| 1b.1 | Add `fix_version` field to XML serializer payload | Pending |
| 1b.2–1b.4 | Version-aware smoke test + answers + comparison queries | Pending |

### Phase 2 — PostgreSQL as primary store ✅ COMPLETE

**Retrieval package (`core/retrieval/`):**

| File | Role |
|---|---|
| `db_retrieval.py` | All SQL — BM25, vector, RRF, trigram, enum lookups |
| `structured.py` | Namespace / identifier / primary_name lookups |
| `lexical.py` | BM25 search with synonym expansion |
| `semantic.py` | pgvector similarity search |
| `crosslink.py` | Relationships, enum lookups, doc chunk enrichment |
| `discovery.py` | Count / list queries + LLM intent classification |
| `reranker.py` | BGE + LLM reranking (entity_row excluded from BGE as of CODE-031) |
| `answer.py` | Answer synthesis, display labels |
| `router.py` | Entry point — RRF-based retrieval, confidence threshold. **Structured planner branch removed (CODE-030)** |

### Phase 3 — BBG ingestion and retrieval

| Step | Description | Status |
|---|---|---|
| 3.1–3.9 | BBG ingest, RRF hybrid search, extended FIX smoke tests | Done |
| 3.11 | Fix collection name mismatch (CODE-010) | Pending |
| 3.12–3.13 | Ingest full BBG dataset (69k rows) + re-run smoke tests | Pending |
| 3.15–3.30 | Obsidian folder-as-category, LLM intent routing, schema-driven headers, payload enrichment utility, all serializer verification | Done |
| 3.37–3.39 | Ingest full PDF / image / astro collections + smoke test | Pending |
| 3.43 | Astro cross-link tagger — RA/Dec proximity + name/alias matching | Pending |
| 3.44 | LLM schema inference: add column population count to prompt | Pending |
| 3.45–3.46 | Cross-link builder manual trigger + background auto-trigger after ingest | Done |

**Key architectural decisions (Phase 3, cumulative):**
- RRF (BM25 + pgvector + trigram) replaces separate merge — single PostgreSQL query, is the primary retrieval path for all non-structured-lookup queries.
- Confidence threshold (`system.json`, default 0.105) scoped to `doc_type == "structured"` only — below threshold returns top-5 "no exact match" instead of a wrong single answer.
- BGE reranker applies to `procedural`, `reference`, `mixed` doc_types only — **not** `entity_row` (CODE-031).
- Structured query planner — **removed entirely** (CODE-030). RRF + reranker path handles all query types now.
- Schema storage: PostgreSQL `schemas` table is the only store (disk JSON retired).
- Domain-specific collections (RECON) need manual schema correction — LLM/heuristic inference reliably fails on business-specific column names like "Moore file name" vs "Tidal Job Name".

---

### Phase 3.5 — Cross-Collection Linking (NER-based)

**Status: design confirmed, not yet built.** Current cross-link discoverer (`cross_link_discoverer.py`) only supports:
1. Exact identifier match (confidence 1.0, skips short numeric IDs)
2. Name/primary_name trigram similarity (>0.3)
3. `_meaningful_context()` guard — a candidate mention must appear with ≥8 other distinct words in a 120-char window to count (CL-02, already implemented)

**Mention-based matching is intentionally disabled** — broker/client names (Goldman, Moore, CITI) are too short/common and produced too much noise (161 low-confidence `recon_assist_file → obsidian` links, most false positives from the word "Goldman" appearing in unrelated OCR text). Confirmed via direct inspection: `gsact.txt → 4.2 Checking Files` link existed with zero actual mention of `gsact.txt` in the target note — the match fired on the word "Goldman" (source `type` field) appearing elsewhere in that note.

This is why `recon_assist_file → obsidian` and `obsidian → recon_assist_file` currently have **zero** confirmed cross-links (verified this session — `discover_cross_links('recon_assist_file', ['obsidian'])` returns 0 candidates under the current strategy set). True cross-linking here requires NER:

| Step | Description | Status |
|---|---|---|
| 3.5.1 | `config/cross_links.json` — KB↔recon link definition with all match fields | Pending |
| 3.5.2 | `core/cross_link_tagger.py` — NER tagger, builds entity dict from Postgres at ingest | Pending |
| 3.5.3 | `doc_serializer.py` — hook tagger into chunking pipeline post-chunk | Pending |
| 3.5.4 | `router.py` — query-time ref tag lookup + linked record fetch | Pending |
| 3.5.5 | Stage 3 synthesis — unified LLM answer from multi-source context | Pending |
| 3.5.6 | Re-ingest kb_docs + obsidian with NER tagging active | Pending |
| 3.5.7 | Ground-truth test: KB article + Goldman recon records surface together | Pending |
| 3.5.8 | Extend to FIX cross-links | Pending |
| 3.5.9 | FIX message interpretation — text input → plain English summary | Pending |
| 3.5.10 | Missing file check — Chat tab, Postgres NOT IN query | Pending |

---

### Phase 3.6 — Data Preparation Tab
| Step | Description | Status |
|---|---|---|
| 3.6.1–3.6.5 | Data Prep tab, two-file join, fuzzy matching, export, Messier+OpenNGC merge | Done |
| 3.6.6 | Astro filename parser fix — target extraction without frame type prefix | **Done** (ASTRO-001) |
| 3.6.7 | RA/Dec cross-link tagger — match astro files to catalog within 0.5° tolerance | Pending |
| 3.6.8 | Name/alias cross-link — match filename tokens against catalog common names | Pending |
| 3.6.9 | Excel (.xlsx) upload with sheet selector + forward-fill for merged cells (CODE-025) | Pending |
| 3.6.10 | PostgreSQL chunk editor for fixing ingestion errors (CODE-026) | Pending |

---

### Phase 4 — MCP Server + Metadata Query Path
| Step | Description | Status |
|---|---|---|
| 4.1–4.5 | Design + implement + smoke test MCP server (local + online modes) | Pending |
| 4.6 | Metadata query path — direct Postgres queries for aggregation intents | **Done** — see Metadata SQL Query Path section below |
| 4.7 | Open WebUI evaluation | Pending |

---

## Metadata SQL Query Path (Phase 4.6 — this session)

`core/metadata_query.py` (new). For `discovery_count` / `discovery_list` intents, the router now tries `run_metadata_query(collection, question)` FIRST; the retrieval-based discovery engine is the fallback. Answers real counts/lists/group-bys via parameterized SQL against `chunks` — fixes the entire AG failure bucket (retrieval-capped counts).

**Pipeline (all steps data-driven, zero hardcoding):**
1. `_collection_fields()` — actual payload keys (sampled 200 chunks) + real table columns; every LLM-proposed field validated against this set, otherwise fall back to discovery.
2. `_field_values()` — distinct values for low-cardinality (≤20) fields, injected into the extraction prompt so filter values are grounded in real data ("Filter values MUST be copied exactly from the listed values").
3. `_extract_spec()` — LLM returns `{operation: count|count_distinct|list_distinct|group_by, target_field, filters[{field,op(equals|contains),value}]}`. count_distinct defaults to `identifier` (the schema's unique key) unless the question asks for a specific field's distinct values.
4. `_best_value_field()` — for group_by/list_distinct: embeds the question against **concept vector group labels** per field (raw-value embedding failed — job names lexically beat broker names) and overrides the LLM's target_field **only when the LLM picked a schema-generic field** (that collection's primary_name/identifier per its schema). This guard fixed "what dates were images taken" (LLM's correct `date-obs` pick no longer overridden to folder_path).
5. `_concept_label_filter()` — when the spec has NO filters, matches the question against concept labels (sim ≥0.6, margin ≥0.05 over runner-up) and injects an equals filter. Fixed "how many FIX tags" → `identifier_namespace=tag` → 947.
6. Filter regrounding — a filter value exactly matching a listed field value is rewritten to `equals` on that field. **Determinism fix:** the LLM's own field is checked first (no rewrite if it already contains the value); only then other fields in sorted order. Was the root cause of 146/34/301/172 answer variance for the same question.
7. Zero-result repair — if the filtered count is 0, drop individual zero-yield filters and keep ones that alone yield rows (LLM sometimes ANDs an invented junk filter onto a good one).
8. Answer sentences built deterministically (`"{n} records match."`, `"There are {n} matching {tf}(s)."`); list_distinct collapses all-ISO-timestamp value sets to unique dates.
9. `chat_engine.py` bypass — `method == "metadata_sql"` results are returned verbatim, skipping the conversational LLM wrapper (which misread bare numbers as tag IDs / lacking context).
10. `discovery.py` intent prompt — "which X has the most Y" now classifies as `discovery_list`, reusing the same hook for group-by questions.

**Standalone verification (5/5):** KB articles=178, FIX-mention articles=48, FIX tags=947, prime-broker list via `type` field, Goldman group-by → Goldman: 16.

**Known limits:** MQ-01 (LLM spec variance), MQ-02 (chat fan-out variance), CODE-024 (NULL identifiers undercount contains-filters).

**Standing constraint: NAS-AI is fully local. No external AI or cloud model connections, ever.**

---

### Phase 5 — Advanced features
| Step | Description | Status |
|---|---|---|
| 5.S1 | LLM-driven schema inference — already done (structured_roles.json replaced by LLM role assignment) | Done |
| 5.M1–5.M7 | Model routing (assign different LLMs per doc_type/query_type in LM Studio) | Pending |
| 5.NEW | Revisit CV-03b label consolidation with larger/better model — current Qwen 2.5 14B over-merges distinct topics even with a guarded prompt | Pending |

---

## Concept Vector Improvements (this session — CV series)

| ID | Area | Description | Status |
|---|---|---|---|
| CV-01 | concept_vector_builder.py | Added `section_heading` as group field for `mixed`/chunked doc collections (docx, pdf) when >1 distinct section exists. | **Done** — docx_test: 1 filename cluster → 9 section-based clusters (Executive Summary, Introduction, Benefits, etc.) |
| CV-02 | concept_vector_builder.py | For collections with a `tags` schema role populated on ≥20% of chunks: Tier 1 groups by the most distinctive shared tag (inverse document frequency), filtering tags shared by >30% of rows as "too broad." | **Done** — kb_docs: 4 useless filename clusters → 126 meaningful topic clusters (`Broadcaster is down or inactive`, `Bad Dates`, `Copy FIX Logs`, etc.) |
| CV-03 | concept_vector_builder.py | Tier 2 for chunks with no usable tag (after CV-02 filtering): TF-IDF top-10 terms per chunk → batched LLM call (30/batch) assigns a shared topic label. Falls back to `Uncategorized` if sklearn unavailable. | **Done** — kb_docs: 5 untagged articles correctly labeled via 1 LLM batch call |
| CV-03b | concept_vector_builder.py | Attempted LLM consolidation pass to merge synonymous labels (`AutoSys`/`AutoSys Job`, `Create KB Article`/`Creating Knowledge Base Articles`) after CV-02/03. | **Reverted** — Qwen 2.5 14B produced unacceptable false merges (`QA Archive→PROD Archive`, `Repo Collateral→Recon Missing Files`) even after prompt tightening (explicit environment/stage warning, "when unsure don't merge"). Function `_llm_consolidate_labels` left in file, unwired. Revisit with larger model in Phase 5. |
| CV-04 | concept_vector_builder.py | Post-clustering relabel pass: any `group_value` that's a filename, ≤3 chars, boolean-ish, pure numeric, or a configured generic term gets replaced with an LLM-generated 2–5 word label built from that cluster's anchor texts. Cached per label to avoid duplicate LLM calls; uses running cluster_id ≥900000 to avoid unique-key collisions. | **Done** — astro_catalog: cryptic NGC type codes (`Gb`, `OC`, `PN`, `?`, `-`) → readable labels (`Galactic Globular Clusters`, `Open Star Cluster Data`, `Planetary Nebula Catalog`, `Unidentified Data Entries`) |
| CV-05 | concept_vector_builder.py | Removed `doc_type`-based branching (`_is_entity_row_collection`) entirely. Tag-path routing now purely data-driven: step 6 of `_get_group_field` checks whether ≥20% of a collection's chunks have a non-empty `tags` field, independent of doc_type. | **Done** — fixed a latent bug where `astro_catalog` (doc_type=entity_row, 13,336 chunks, 0% tagged) would have routed through the tag path and triggered ~445 Tier-2 LLM batch calls on every rebuild; now correctly routes through `type` grouping with zero LLM cost. Verified via dry-run comparison before applying. |

**Cascade order in `_get_group_field` (final, as of this session):**
1. `folder_path` (obsidian)
2. `section_heading`, if >1 distinct value (docx, pdf — CV-01)
3. `identifier_namespace`, if >1 distinct value (xml_test)
4. `type`, if >1 meaningful (non-data-type, non-numeric) value (recon_assist_file, astro_catalog)
5. `category`
6. `tags`, if populated on ≥20% of chunks (kb_docs — CV-02/03, CV-05)
7. `source_file` fallback → CV-04 relabels resulting clusters via LLM

---

## Cross-Link Improvements (this session — CL series)

| ID | Area | Description | Status |
|---|---|---|---|
| CL-01 | cross_link_discoverer.py | Raise minimum identifier length in mention matcher to reduce false positives from short generic terms. | **Superseded** — mention matching was removed from the discoverer entirely (not just length-gated); see Phase 3.5 note. Current discoverer only does exact-identifier + name-similarity matching. |
| CL-02 | cross_link_discoverer.py | `_meaningful_context()` guard — a mention must appear with ≥8 other distinct surrounding words to count as a real discussion, not a passing/list reference. | **Done** — already implemented in current discoverer (kept in place even though mention matching itself is disabled, ready for reintroduction if needed) |
| CL-03 | cross_link_discoverer.py / new tagger | NER-based cross-linking (Phase 3.5) — scan chunk text for known identifiers from other collections at ingest time, validated against live Postgres entity dictionary. | Deferred — Phase 3.5, full design above |
| CL-04 | router.py | Wikilink traversal — when surfacing related sections for a structured-collection hit (e.g. `gsact.txt`), follow one hop of confirmed obsidian wikilinks from any linked obsidian note, to surface e.g. `4.2 → 4.3.1 Checking sFTP for Activity`. | Pending — next priority |
| CL-05 | — | `obsidian → recon_assist_file` and `recon_assist_file → obsidian` cross-links: confirmed **zero** produced under current discoverer strategies (verified this session). Needs CL-03 (NER) or a manual identifier hint list. | Deferred — Phase 3.5 |

**Verified working now:**
- `obsidian → obsidian` wikilinks: 43 confirmed, confidence 1.0, `match_type='wikilink'`. Source: `core/related_titles_linker.py::persist_related_titles_as_crosslinks()`, reads `related_titles` from chunk payload (populated at ingest from `[[wikilink]]` syntax), wired into background runner after ingestion.
- Knowledge Graph tab cross-link review: grouped by `(target_collection, target_identifier, match_type)` with resolved display names (not raw chunk IDs/row numbers) — Confirm All / Reject All / Reject+Ignore-term apply to the whole group at once instead of per-row.

---

## Chat Engine Improvements (this session)

| ID | Area | Description | Status |
|---|---|---|---|
| CHAT-01 | chat_engine.py | Removed hardcoded FIX tag regex (`\btag\s+(\d+)\b`) from Tier 1 identifier matching — was domain-knowledge hardcoding that also failed on multi-tag questions ("tags 453, 452, and 448"). | **Done** — Tier 1 now only matches explicit filenames; tag/ID routing handled by Tier 2 LLM via `routing_description` |
| CHAT-02 | chat_engine.py, collections.json, ui (collection form) | Two-tier collection routing: mandatory `routing_description` field (entered at collection creation, describes domain/topics) + auto-appended concept vector cluster topics for doc-type collections. Structured collections use static description only. | **Done** — verified: "what is FIX tag 22" → xml_test first; "broadcaster issue in CRD" → kb_docs first; "gsact.txt Goldman recon" → recon_assist_file first |
| CHAT-03 | chat_engine.py | Tier 1 identifier hits no longer return early / override Tier 2 ordering — merged in AFTER the LLM's procedural-vs-lookup judgment, so "how can I check if gsact.txt is on sftp" correctly prioritizes obsidian procedure notes over the raw structured record, while still including recon_assist_file as a fallback. | **Done** |
| CHAT-04 | chat_engine.py | `augment_query_with_focus` no longer injects stale conversation-history terms into questions that already contain their own filename/tag identifier — was causing "what is FIX tag 22" to get polluted with a prior turn's `gsact.txt` reference. | **Done** |
| CHAT-05 | chat_engine.py | Hallucination fix: when `primary_answer` is literally "No answer found", skip the LLM wrapper entirely (it was fabricating plausible-sounding note titles/content) and return a clean "couldn't find" message instead. | **Done** |
| CHAT-06 | chat_engine.py | Filter concept-link related sections from structured (`doc_type` uniformly `structured` across a sampled 5 chunks) collections when merging into another collection's answer context — was leaking irrelevant `bbg_fields`/`xml_test` "related" sections into unrelated RECON/obsidian answers. | **Done** |
| CHAT-07 | — | Multi-item queries ("tags 453, 452, and 448") still only retrieve one item. | Pending — see CODE-023 |

---

## Structured Planner Removal (this session)

Confirmed via live testing that the structured planner (`structured_query_planner.py` + `structured_plan_executor.py`) was configured with `dry_run: true` in every observed run — it generated a plan via LLM call on every query but never executed it, adding pure latency with zero functional contribution. Fully removed:

- Deleted `core/structured_query_planner.py`
- Deleted `core/structured_plan_executor.py`
- `core/retrieval/router.py`: removed both imports, `_get_structured_planner_config()`, the entire planner branch (former step 4), `skip_planner` parameter, `structured_plan_dry_run` from the response dict, and the `structured_query_plan` debug panel case
- `ui/system_config.py`: removed `_structured_planner_section()` and its call site
- `core/chat_engine.py`: removed `skip_planner=True` argument (parameter no longer exists)

**Post-removal bug caught and fixed:** the removal initially left a dangling `return response` (from the old step-6 fallback) with `response` no longer defined, since steps 5 (discovery) and 6 (semantic/lexical fallback + cross-link/concept-link enrichment) had been accidentally deleted along with the planner block. Restored both steps intact. Verified via full smoke suite after fix: FIX 13/13, RECON 10/10, KB 10/10, OBS passing.

---

## BGE Reranker Fix (this session)

Root-caused a wrong-answer bug where `run_query_with_method('kb_docs', 'how to check recon files on sftp')` returned the wrong KB article despite RRF correctly ranking the right one #1. Traced through: RRF ranking (correct) → BGE reranker with `top_k=25` (introduces the error) → BGE reranker with `top_k=10` on the same query (correct). Confirmed with direct reproduction: `bge_rerank()` given the top-10 RRF candidates preserved correct order; given the top-25 candidates, it promoted a candidate with 10x lower RRF score to #1.

Fix: removed `entity_row` from `system.json` → `bge_reranker.apply_to_doc_types`. KB docs now rely on RRF ranking alone (already correct) plus the LLM reranker path only if separately enabled. `structured`, `procedural`, `reference`, `mixed` doc types unaffected.

Also confirmed via this investigation: `nlp_text` (dedicated PostgreSQL column) IS correctly populated with full chunk text for kb_docs and used for BM25/tsvector — an earlier concern that `payload->>'nlp_text'` was empty was a red herring (that JSON key is simply never populated; the real column is).

---

## Schema / Ingestion Findings (this session)

- **KB CSV → `kbtags` column**: schema-mapped to a `tags` role. Payload key was `kb_tags`, renamed to `tags` for consistency with other system-convention payload keys (`primary_name`, `description`, `identifier`). Required: `table_serializer.py` line ~220, `concept_vector_builder.py` references, `diag_verify_ingestion.py`, `diag_concept_fields.py`. Re-ingested kb_docs after rename.
- **KB CSV → `KBinactive` column**: confirmed working correctly via the existing Field/Row Filters UI (`exclude_equals` / `TRUE`) — every inactive article has an active duplicate-titled replacement; 21 of 199 rows correctly excluded at ingest, verified by ID (not just title) cross-check.
- **KB CSV → `KBtype` column**: boolean flag (0/1), not a usable category — correctly excluded from grouping consideration by the `_data_types`/pure-numeric filter in `_get_group_field`.
- Halo KB API JSON structure reviewed (`kb_tags`/`tag_string`, `description`, `resolution_list`, inline `Open image-XXXXXXXX.png` references requiring a separate attachment-fetch endpoint) — informs the future Phase 4 Halo MCP ingestion adapter design. Image/attachment pull from Halo is a separate, not-yet-scoped capability.

---

## Smoke Test Results (2026-07-03/04, post structured-planner-removal + BGE fix)

- FIX: 13/13 ✅
- RECON: 10/10 ✅
- KB: 10/10 ✅ (includes previously-failing KB-06 sftp, KB-07 PB folders — root cause was BGE reranker, not a smoke test authoring error, though two genuinely wrong `must_contain` expectations were also corrected: `PrimeBroker`→`Prime Broker`, added `US1-Proc02`)
- OBS: passing

---

---

## Session 2026-07-05/06 — Multi-item chat, schema inference overhaul, metadata-query hardening

**All smoke suites green at session end: FIX 13/13, RECON 10/10, KB, OBS, BBG, multi_item 18/18.**

### Multi-item chat queries (CODE-023 — RESOLVED)
- `chat_engine.py`: deterministic gate (`_is_multi_item_candidate` — ≥2 identifier tokens + list separator, zero LLM cost for single-item), LLM splitter with grounded validation (every sub-question must contain a real token; cap `MULTI_ITEM_MAX=5`; fail-safe no-split), parallel per-item fan-out, merged per-item answer (structured verbatim, doc synthesized, missing = clean not-found).
- `router.py`: new `single_item` param — a split sub-query targets exactly one identifier by construction, so discovery/comparison intent classifications are overridden to `answer` (fixed plural-phrasing sub-questions being routed to discovery).
- New test: `tests/smoke_multi_item.py` (gate 9 offline cases, splitter 4 live cases, 5 e2e chat cases incl. strengthened REG-02 that cannot false-pass on not-found).

### Reference-identifier / alias searchability (new — fixes DL-02/DL-05 class)
- `table_serializer.py`: reference_identifier role values → `reference_identifiers` payload list + namespaced link_keys.
- `nlp_generator.py`: reference values appended to structured nlp_text ("References: ...").
- `db_retrieval.get_by_identifier`: on miss, exact fallback against `reference_identifiers`/`aliases` payload arrays — a record is findable by ANY of its exact names.
- chat Tier 1: queried `payload->>'identifier'` (never populated) — now the real `identifier` column + the payload arrays; dead procedural-skip block removed.
- `run_parallel_queries`: empty-answer detection expanded ("Found 0 item(s)", "0 record(s)", etc.) — zero-result answers no longer win the fan-out.

### Metadata SQL query path — variance armor (MQ-01/MQ-02 mitigations)
All deterministic, in `metadata_query.py`:
- **Tautology guard**: list/group target colliding with an equals-filter field → retarget to identifier.
- **Degenerate-result fallback**: empty list / filter-echo list / empty group_by → return None → discovery engine.
- **Record listings**: identifier target lists `identifier — primary_name`; primary_name target gets truncated description companion; markdown-safe (code spans, real bullets); every answer states its filters.
- **Preemptive concept-vector override scoped to group_by only** — it was an unguarded embedding argmax that repeatedly replaced correct LLM picks on list questions (FIX-06/07, astro dates, RECON-06).
- **System-key exclusion** (`_SYSTEM_KEYS`): pipeline bookkeeping (doc_type, type_field, link_keys, ...) no longer offered as filter/target fields.
- **description_fields expansion**: labeled source columns (DataType, Recon Tool File Format, ...) are first-class queryable fields with grounded value lists.
- **Constant-field exclusion**: fields with 1 distinct value collection-wide dropped (source_file in single-file collections).
- **Schema grounding**: extraction prompt now includes role → source-column meanings via `_collection_schema` (merges ALL stored schemas — the old `(collection, collection)` lookup silently missed every real stem).
- **Role-name matcher** (single-schema collections only): question token compact-matches a role's source column ("files" ⊂ "Moore file name") and the LLM's target matches nothing → retarget; tie between roles → identifier wins only when the pick's tokens are a subset of identifier's; self-match protected ("dates" → date-obs kept).
- **Intent-mode coercion**: router passes `intent_mode`; a list-intent question whose spec came out count (or vice versa) is coerced — spec extraction can no longer overrule the upstream classifier.

### Schema inference overhaul (LLM perception + deterministic structure)
- `schema_inference.py`: filename pre-pass now **constrains instead of hides** — filename columns stay in the LLM prompt (pattern broadened: spaces/$/&/multi-dot, verified 87–100% vs 0% separation on live data), allowed roles identifier/primary_name/aliases/reference_identifier, enforced post-hoc; non-near-unique filename in a 1:1 role → reference_identifier; **leftmost near-unique filename key wins identifier ties** (stripped from every role before reassignment — leftover copies emptied identifier via refine_schema_roles dedupe).
- `schema_inference_table.py`: **LLM-primary** (heuristic only when LLM unreachable), **never persists a junk schema** (raises visibly), legacy disk-JSON resurrection removed, `schema_model` config for a dedicated (larger) inference model — wiring verified, config key on the box still to be fixed (no `using schema model` line yet).
- `xml_serializer.py`: `_correct_detail_table_keys` — batch-level parent-key correction (a table containing another table's identifier with ≥80% value overlap, own key unreferenced → detail table keyed by parent; sibling twins whose shared column is each's own identifier don't count as referencing); displaced key → primary_name when vacant; **discriminator correction** (aliases-class column completing a near-unique composite with the parent key → enum_value); merged-twin schema picked by most non-empty roles instead of first.
- `local_llm_client.py`: `max_tokens` cap (config, default 1024 — a runaway generation no longer burns the 300s timeout) + failures now log exception class/message instead of silent None.
- Verification assets: `diag_schema_all.py` (fleet-wide stored-vs-inferred diff, per-source-file, comparison limited to reconstructable columns), `diag_schema_ab.py` (old pure-LLM from git vs current pipeline, scored vs ground truth — result: old era was 2/5 too; identifier/alias correctness came from the old deterministic collapse, not the LLM), `diag_schema_recon.py` (superseded by _all).

### Ingestion & UI
- Ingestion warnings: unreachable source path / no path / 0 files discovered / per-reason skip & fail summaries — orange "finished with warnings" instead of green zeros; per-file Reason column.
- Schema editor: **Tags and Other roles now visible and editable**; save NEVER drops columns (unassigned → other) — the invisible-other bug silently deleted "Recon Tool File Format" from serialization twice.
- Bulk schema delete (multi-select + confirm dialog); single delete refreshes the dropdown.

### Recon collection final state (verified 10/10)
identifier=Moore file name · primary_name=Tidal Job Name (manual pin — proven 14B blind spot, 6+ runs never picked it) · aliases=Prime Broker file name · reference=Date Column to Check, Move Script · type=Prime Broker, Recon Tool Data Source, Active, Included in Email Alert · other=Recon Tool File Format + note column.

### Key learnings (this session)
- **LLM for perception, math for constraints**: every schema/spec failure was an unguarded LLM pick; every fix was either grounding (real values, schema meanings, upstream intent) or a structural invariant (tautology, cardinality, composite keys, leftmost convention). Guards are no-ops when the model is right — model upgrades need no code changes.
- **A saved wrong schema is worse than no schema** — it short-circuits every future ingest. Never persist junk; fail loudly.
- **The UI process caches code** — every fix needs a restart before retesting; half the "still fails" reports were stale processes.
- **LM Studio serves chat + embeddings + schema calls on one queue** — testing during ingestion starves both; schema failures mid-ingest were contention, not code.
- **Retrieval-capped counts as fallback are confidently wrong** — a failed exact count should say "couldn't determine", not return top-k as truth (future item).
- **Union of multi-file schemas is mush** — role-level machinery must scope to single-schema collections or work per source file.

### New/open tickets
| ID | Description |
|---|---|
| BG-01 | background_tasks has no heartbeat — rows stay 'running' forever if the process dies. Startup sweep (auto-cancel orphans) + stale marker |
| UI-03 | NiceGUI "parent slot deleted" noise — background/timer UI updates need liveness guard |
| MODEL-01 | `schema_model` config key not resolving on the box (no "using schema model" line) — fix key/id, then Qwen3-VL-32B Q4 serves schema inference via JIT load |
| DESIGN-01 | Astro/personal collections: workflow profiles (config flags per collection controlling metadata-SQL/count/routing behavior) instead of one pipeline for work + personal domains |
| INGEST-01 | Debug-print cleanup in xml_serializer/orchestrator (DEBUG LINK INDEX block incl. stale "CHECK TAG 22" check, FILES_SEEN/ALL_ROWS/sample-keys, PRE/POST-MERGE 21R2 lines) — gate behind config or delete |
| CTM-01 | CTM XML ingest prep: parser fit check on a sample file first (nested message-style XML vs FIX's attribute-row shape); DTCC specs are login-gated on DTCC Learning, not public |
| DATA-01 | Datatypes_FIX44 has no schema/chunks (LLM failed during its window, fails loudly now) — re-ingest when LM Studio idle, or via schema_model once MODEL-01 fixed |

---

## Session 2026-07-07 — Eval runner, full eval pass, PP-02/NA-03/DL-07 fixes

### Eval automation
- `tests/eval_runner.py` — all 50 eval-doc questions through BOTH surfaces (Ask = run_query_with_method against expected collection; Chat = chat_turn auto-routed), incremental md+json results, latency summary (avg/p95/max + slowest five). Full run: 50.7 min. **This is the regression harness — diff the json between runs.**
- First full run scoreboard: AG 7/10 exact · PR 7/8 Ask · MI Chat 3/4 (was 0/4) · DL lookups solid · failure mass concentrated in Chat routing (kb↔obsidian twins) and XC cross-collection synthesis. Latency: Ask avg 14.8s, Chat avg 46s — the gap is serial LLM call count, not retrieval.

### Fixes from eval failures (each verified via diag + smoke gates)
| Eval ID | Root cause | Fix |
|---|---|---|
| AG-01 | Spec variance produced COUNT(*) = 348 chunks, not 178 articles | metadata_query: plain `count` coerced to count_distinct(identifier) unless question literally says rows/chunks; intent-mode (list/count) passed from router and coerced — spec extraction can no longer overrule the upstream classifier |
| PP-02 | `OrderQty` is ONE tsvector token — lexically invisible to 'order quantity'; typo 'teh' vetoed every AND-variant (websearch_to_tsquery ANDs terms), silencing the whole BM25 leg of RRF | `word_split()` in nlp_generator (camelCase/snake_case split appended to structured nlp_text; applied in BOTH the tables path and the XML serializer's own text build — they don't share code); `_filter_corpus_words` drops corpus-absent tokens before building BM25 variants; search_bm25 standalone gets OR fallback on zero rows. NOTE: an OR-variant in RRF was tried and REVERTED — it flooded the lexical leg and flipped clean queries wrong |
| NA-03 / PP-03 | Ask rendered nearest-neighbor articles with full confidence on weak matches | Low-coverage disclaimer: fraction of question content words present in answering chunk < `answer_coverage_disclaimer_below` (config, tuned 0.5 → 0.3 on three-question calibration) → "⚠ No direct match found" banner. ALL content words count in the denominator (corpus-filtering it inflated 1-of-3 to 100%). Banner must live OUTSIDE LLM synthesis — ask.py strips before, re-prepends after (the LLM summarizes it away otherwise) |
| DL-07 | Chat Tier 1 anchors on filenames only — ALL-CAPS codes (BBG mnemonics, job names) never pinned routing | Tier 1 extended to code-like tokens (`[A-Z][A-Z0-9_]{3,}`) matched against identifier AND primary_name + payload arrays |

### Learnings
- **A too-strict lexical leg fails silent, a too-loose one fails loud**: AND-veto returned zero rows invisibly; the OR experiment returned garbage visibly. Token-level vocabulary filtering (per-collection EXISTS checks) is the precise middle.
- **Any text that must survive LLM synthesis can't live in the text** — flags/banners must travel beside the prose, not in it.
- **Coverage denominators must include the words that can't match** — excluding typos inflated confidence exactly when it should collapse.
- PP-01 ("that goldman activity file...") is deliberately unfixed: it is the acceptance test for the cross-links/concept-vectors block.

### New tickets
| ID | Description |
|---|---|
| VOCAB-01 | Per-collection vocabulary table (ts_stat at ingest) + trigram spell-correction of query tokens (brodcaster→broadcaster) — upgrades PP category from "survives typos" to "understands them"; correct-spelling test confirmed the right answers exist |
| UI-04 | Auth gate for the UI (reachable via Tailscale from phone; no login today) — before Confluence/Halo work data lands |
| SPEED-01 | Chat turn = 4-6 serial LLM calls (46s avg): dedupe per-collection llm_detect_intent (same question, 3 identical calls), fast_model routing for classification calls, merge intent+contextualize+routing into one call, stream synthesis. Target 4-8s on current hardware |

---

## Session 2026-07-07 (cont.) — Cross-links block, chapter 1: restore, consume, navigate

**State found:** cross_links nearly empty (4 NER rows). The 43 obsidian wikilinks were lost when a UI restart killed the background threads mid-queue after the all-collections re-ingest (BG-01 upgraded: needs startup sweep + RE-QUEUE of interrupted work, not just orphan relabeling). Raw material had survived (701 chunks with related_titles).

**Restored:** `rebuild_cross_links.py` extended with the missing wikilink-persistence pass. Recovery run: 43 wikilinks confirmed + 95 NER mention links (kb_docs 16, obsidian 79 — the first-ever recon↔notes bridge; quality sampled clean, all confirmed via KG review). NER found both filename AND job-name mentions — the searchability overhaul feeding it.

**Consumed:**
- `get_cross_links_for_identifier` + chat Tier 1 cross-link routing made BIDIRECTIONAL (links are edges, not arrows — obsidian→recon links now surface when answering from recon).
- Chat related_sections UN-suppressed (they were hardcoded to [] from the noise era), ranked confirmed-first (exact/ner/wikilink → hop → similarity → concept), capped at 5 (`RELATED_MAX_SECTIONS`).
- Lead sentence labels the name field: "gsact.txt — Tidal Job Name: 019_W_..." instead of the alias-implying "is" (needs `primary_name_field`, now emitted by table_serializer).

**Navigable (PP-05 wish granted):**
- `/entry/{chunk_id}` full-article page (ui/entry_page.py): sibling chunks merged (identifier for kb articles, source_file for notes, single for structured), ALL images inline; "Open full article ↗" link on every related section (anchor_chunk_ids attached in both router link paths).
- `_md_safe` underscore escaping outside code spans in all markdown rendering — job names/aliases no longer italics-eaten.
- Import gotcha fixed: `import ui.entry_page` shadowed nicegui's `ui` in app.py — `from ui import entry_page`.

**Acceptance:** XC-01 ✅ both surfaces — record + 4.1 (NER confirmed) + 4.2 (wikilink_hop; CL-04 traversal was already wired, it just never had confirmed links to walk). PP-01 still ❌ (Tier 2 routed to bbg_fields this run) — REMAINS the acceptance test for chapter 2.

**Chapter 2 (next):** concept-vector quality (bbg 169 hierarchical-path clusters; recon clusters polluted by CODE-024 merged-cell fallthrough incl. source-data typo 'Transction'), kb↔obsidian twin routing, Tier 2 stability. Also: KG review grouping collapse (UI-05, cosmetic dup rows), preview-images gap (markers stripped from previews — mitigated by full-article links).

---

## Session 2026-07-07 (cont.) — Chapter 2: deterministic routing + cross-collection composition

### Tier 1.5 — concept-centroid routing (chat)
Dry-run on 8 eval questions justified it: centroid similarity ranked the right collection in 5-6/8, including three the Tier 2 LLM was failing (PP-06, DL-07, XC-05). Implemented in `select_collections`: one question embedding vs ALL collections' concept-vector centroids (single pgvector SQL); when the leader clears `centroid_routing.min_sim` (0.6), collections within `margin` (0.08) are selected (max 3) and **the Tier 2 LLM call is skipped entirely** — deterministic routing + one less LLM call per turn (SPEED-01's first win). Murky cases fall through to the LLM exactly as before. Tier 1 anchors merge in and are never dropped by the cap. Verified: PP-06 ✅ (was ❌), DL-07 chat ✅ (was ❌), DL-08 held, multi 18/18.
Twins bonus: kb/obsidian centroid scores sit adjacent (0.769/0.775) so top-3 naturally includes both; the fan-out's best-answer pick arbitrates.

### Tier 1.25 — identifier-prefix anchors (chat)
Question content words (config noise lists excluded) that prefix-match a collection's **identifier column** pin that collection like a filename does ('jpm' -> jpm_cfd_position, 'citi' -> citi_dcm_fees.csv). identifier column ONLY — matching primary_name routed xml via 'check' -> CheckSum during calibration.

### Cross-collection composition (XC bucket structural gap — closed)
Raed's design, better than the LLM-context merge first proposed: **compose, don't blend.** Runner-up STRUCTURED answers (records/metadata only — never a second procedure doc, which invites step-blending; see PR-03 💀) are appended verbatim AFTER synthesis as "**From {collection}:**" blocks — the LLM never sees them, zero contamination. Doc runner-ups remain related links. Includes the "No exact match — closest results" record listings (that's where the file lists live). Relevance gate: a question word must be the PREFIX of a LISTED IDENTIFIER — names/descriptions excluded ('check' in 'CheckSum', 'date' in a field description = coincidence, not evidence). Router fixes en route: the no-exact-match branch now sets `_last_answer_payload` (was unclassifiable downstream) and `_last_answer_payload` resets per call (stale-payload leak across questions — latent cross-link-enrichment bug).
**Verified: XC-02 ✅ (procedure + jpm records + 090_W_RECON_JPM_PB_PULL), XC-05 ✅ (workflow + citi file records), bbg/xml noise gated out, single-item + multi 18/18 unaffected.**

### Lesson (paid for twice tonight)
Loose string matchers MULTIPLY: a prefix-match on names ('check'->CheckSum) routed noise in, and a substring gate ('check' ⊂ 'checksum') waved it through. Identifier-anchored, prefix-only matching composed cleanly on the first try. If a deterministic guard needs a third refinement round, disable it behind config and take it to the design board.

### Cosmetics parked
`- : ` empty rows in record listings = CODE-024 NULL-identifier rows; stray low-ranked concept links (correctly demoted to list bottom by confirmed-first ranking); KG review grouping dup rows (UI-05).

---

## Eval re-baseline 2026-07-08 (results/eval_run_20260708_122759) — vs overnight 07-07 run

**Flipped to ✅ (Chat):** PP-02 (both surfaces — camelCase split + typo-proof BM25 stack), PP-04, PP-07 (right runbook), PR-01, PR-03 (was 💀), DL-07 (routed [bbg_fields] alone — Tier 1.5 + code anchor), XC-05, XC-06, NA-01, NA-04. DL-02/XC-01 carry the new "— Tidal Job Name:" labeled lead sentence. Ask surface stable-or-better throughout; NA-03/PP-03 wear the low-coverage banner as designed.

**Regression cluster — AG-chat (07/08/09 ❌, were ✅; noise blocks on 01/03): ONE mechanism.**
Centroid routing ranks kb/obsidian above recon/xml for aggregate questions (doc-cluster gravity on words like "recon files", "brokers"), and the fan-out's best-pick cannot tell a WRONG metadata answer from a right one — a confidently non-empty junk count from the wrong collection beats the right group-by. The old LLM router won these by luck. MI-02 chat shares the disease (sub-questions answered from kb instead of recon).
**-> Chapter 3 lead item: metadata-answer arbitration** — when multiple collections return metadata_sql answers, judge them (grounded-filter-references-question-token beats unfiltered; listing beats bare count; else routing order). Acceptance: AG-07/08/09 chat, MI-02.

**Hotfixed immediately:** counts never compose into secondary blocks (only record listings do) — AG-01's "From obsidian: 8 matching..." had gated itself in via lead word 'There' prefix-matching question word 'there'. The gate now requires listing lines.

**Also carried to chapter 3:**
- DL-05 Ask still `semantic` despite the _record_covers_question compact fix — needs a direct trace of the filename branch (warm-up task).
- PR-08 chat wrong-twin (kb over obsidian) — twin ranking within fan-out.
- Latency: Chat avg 46 -> 52s (composition + wider fan-out); PP-01 at 272s is SPEED-01's poster case. Ask stable at ~15s.
- PP-01 unchanged ❌ — remains the block's acceptance bar.

---

## Session 2026-07-08 — Chapter 3: arbitration ladder, anchor family, third eval baseline

### Arbitration ladder (run_parallel_queries best-pick — was "first non-empty wins by routing order")
Deterministic score tuple per candidate answer:
1. **Method rank**: exact-key (identifier/namespace/enum/relationship) > metadata_sql > doc/semantic.
2. **Collection-name hit** (data answers only — muted for docs so "recon file missing, what do I do" keeps its runbook).
3. **Graded groundedness** for metadata: EQUALS-filter value matching a question token (0) > contains (1) > ungrounded (2). "Equality is a claim, substring is a shrug."
4. **Doc-vs-doc rung** (twins): among document answers, TITLE word-hit count decides ('Charles River Log Folder for Errors' 4 hits beats 'CRD Logs Archive' 2) — fixes PR-08 while DL-08/PP-04/PP-08 hold.
5. Routing order = final tie-break only.
Fixed en route: sentence period riding into the captured filter value ('= tag.') ungrounded AG-03's perfect answer — rstrip('.').

### Anchor family (select_collections — things the user LITERALLY wrote, checked against the system's own data)
- Tier 1: filenames + ALL-CAPS code tokens (identifier + primary_name columns).
- Tier 1.25: word prefix-matches record identifiers ('jpm' -> jpm_cfd_position). Identifier column ONLY (primary_name matching routed xml via 'check'->CheckSum).
- Tier 1.2: question token ⊂ collection name ("in the recon file" -> recon_assist_file).
- Tier 1.2b: schema COLUMN names, unique-to-one-collection only, name-class roles only, proportionality rule (multi-word column needs ≥2 of its words — 'prime brokers' earns 'Prime Broker'; 'moore' alone does NOT earn 'Moore file name'; single-word columns need ≥5 chars — 'Mnemonic' yes, 'Name' no), adjacent-word joining ('filenames' covers 'file'+'name').
- Tier 1.5 merge bug fixed: successive anchors were overwriting the same last slot (recon evicted by a later anchor); anchors now fill distinct slots and never evict each other.

### metadata_query hardening
- **Field-name-valued filter drop**: type = 'Prime Broker' (column name as value) = LLM confusing schema with data — dropped at spec time.
- **Value-anchor injection**: question token EXACTLY equal to a listed field value -> equals filter injected deterministically ('tags' -> identifier_namespace = tag; 'goldman' -> Prime Broker = Goldman). Kills the AG-03 variance class — the embedding-based concept filter misses on bad runs, token=value equality never does.
- **Zero-repair extended to lists, then PARTIALLY REVERTED**: dropping ALL zero-result filters turned NA-04's no-answer trap into a 200-row dump. **Lesson: repair heuristics must distinguish junk filters (schema confusion — handled by the field-name drop) from honest-but-unmatched filters (the ANSWER is 'nothing matches').** Partial keep-repair stays; drop-all removed.
- **Array-element expansion**: list_distinct on jsonb-array fields (aliases, reference_identifiers) lists elements, not raw JSON strings.

### Third eval baseline (results/eval_run_20260708_202253) — best on record
DL 10/10-grade (DL-05 finally identifier_lookup with the alias — _record_covers_question haystack now includes FIELD LABELS: 'filename' lives in 'Moore file name', not in any value). MI-02 chat ✅ (both records verbatim — ladder rank 0). AG-01/02/03*/04/07/08 chat ✅ (*after the period fix + value-anchor). PR-02 held by design. NA-04 restored post-revert. Remaining fails all named: PP-01 (bar), PP-03 (VOCAB-01), PR-08 (fixed post-run, doc rung), MI-04 (partial — full fix needs heterogeneous splitter), AG-10 (Phase 1b fix_version), astro set (DESIGN-01, deprioritized), XC-03-class dual-intent questions (splitter extension, design item).

---

## Eval baseline #4 (results/eval_run_20260709_023135) — PP-01 SOLVED

**PP-01 Chat ✅ — the acceptance bar fell without its planned assault**: the doc-title
arbitration rung picked obsidian's "4.1 Checking the Tidal Recon Job" (title hits) over
kb's TRAIANA doc that had won by routing order for three runs; answer quotes both
Goldman tidal jobs verbatim from the 4.1 table. Also newly ✅: DL-10 Chat (actual NGC
2064 image record via exact-key arbitration), XC-04 Chat (M51 image confirmed with
record), NA-02 Chat (honest not-found, bbg junk gone), PR full sweep 8/8 (PR-08 held by
the title rung). Tally ≈44/50 acceptable (baseline #1: ~25). Chat avg 48.6s — PP-01 at
293s is SPEED-01's exhibit A. Cosmetic fixed post-run: duplicate filters in metadata
answers (value-anchor + regrounding mirroring) — deduped. New watch-item: XC-06 Chat
wobbled to a lesser broadcaster note (one debug run when convenient).

## Next Session Priorities

0. **VOCAB-01** — per-collection vocabulary (ts_stat at ingest) + trigram spell-correction of query tokens; fixes PP-03 (the last PP fail), feeds corrected text to BM25 AND centroid routing.
1. ~~PP-01 assault~~ — **resolved by the doc-title rung** (baseline #4). Residual: its 293s latency belongs to SPEED-01.
2. **AG-10 / Phase 1b**: fix_version payload field at merge-serialization -> version-scoped metadata filters.
3. **Heterogeneous multi-item splitter** (MI-04 full, XC-03-class dual-intent questions). Design note from MI-04 testing: extend the multi-item GATE to accept type-role VALUES as items — 'goldman'/'jpm' are listed Prime Broker values, so "X for goldman and jpm" splits into two value-anchored sub-questions, each answerable by the metadata path with its injected equals filter.
4. MODEL-01 (schema_model config), CODE-025 ffill, astro/DESIGN-01, then SPEED-01, then Confluence/Halo ingestion + memory phases.
1. **MODEL-01** — fix `schema_model` config; re-ingest Datatypes_FIX44 (DATA-01) as the 32B's first live test before CTM.
2. **Eval retest** (NAS_AI_Retrieval_Eval_v1.md) — full pass: MI bucket should flip to ✅; DL-02/05, AG/RECON buckets re-scored; XC remains the largest unaddressed group.
3. **Chat routing hardening** — DL-08/PP-03 no-collection fallback; kb_docs/obsidian wrong-twin ranking.
4. **CL-04** — wikilink traversal in router (4.2 → 4.3.1 chain).
5. **CODE-025/026** — Data Prep Excel upload + ffill (fixes CODE-024 Goldman NULL row), PostgreSQL chunk editor.
6. **CTM-01** — sample-file parse + schema dry-run when the file is ready.
7. Honest count fallback ("couldn't determine" instead of retrieval-capped number); BG-01/UI-03/INGEST-01 cleanups.
8. Phase 3.5 NER cross-linking (CL-03/CL-05); full BBG ingest (69k); CV-03b revisit with the 32B; astro synthesis (DL-06/DL-10); DESIGN-01 workflow profiles.
---

## Session 2026-07-09 (cont.) — VOCAB-01 delivered, honesty guards, portability watch

**PP-03 SOLVED — PP bucket 8/8.** Full VOCAB-01 chain live: 'brodcaster acting up agian'
-> surgical correction ({'brodcaster': 'broadcast'}, stopword+lexeme guards), kb_docs
routed by centroid (0.657), "Message Broadcaster Down" retrieved via the solo-variant
BM25 fix (AND-variant 'broadcast & acting' had excluded it from the whole candidate
pool). Coverage banner now judged on CORRECTED words (a corrected answer must not wear
the typo's banner). smoke_recon 10/10 post-changes.

**Concept-link noise fixed (the "whacky related" item).** diag_concept_dist measured the
truth: centroid-vs-centroid ambient median ~0.60, genuine links >=0.81 with margins
>=0.17, noise living in the 0.75-0.80 band (the Jennison-0.78 card on a broadcaster
answer). find_concept_links now requires sim >= min_sim AND sim >= target collection's
own median + margin (config concept_links {min_sim 0.80, margin 0.15}); median computed
live -> self-calibrating. Related section only; zero answer-path surface.

**Value-groundedness guard (metadata_query).** The LLM force-mapped 'Barclays' -> BOA
(nearest listed bank) despite the prompt forbidding invention. Deterministic guard: an
equals-filter value must overlap the question (raw substring | token overlap | token-in-
compact). Ungrounded -> dropped; if nothing remains, metadata path aborts to retrieval
(honest banner) rather than dumping everything. Goldman/tag filters untouched (grounded
by construction).

**Named-collection primacy (arbitration).** "which RECON files come from Barclays" had
kb_docs' contains-mention headline over recon's honest empty. Now: exactly one collection
named in the question + named collection EMPTY + winner only metadata-CONTAINS/ungrounded
(mrank 1, grounded>=1) -> honest zero headlines, displaced winner composes below
(unconditionally — it earned its arbitration score). Exact-key and grounded-EQUALS
winners keep the headline; doc winners untouched (the 'moore' hijack lesson). Keys off
the live collection list -> inert if data is reorganized.

**NA-04 replaced**: old corpus-wide Barclays question expired (data now contains
Barclays mentions — honest matches, not a regression). New trap: "which recon files
come from Barclays" (collection-scoped honest zero).

### Portability watch (are we designing around this dataset?)
1. **Similarity floors are corpus+model-measured**: centroid routing 0.6/0.08,
   concept_links 0.80/0.15, vocab 0.75. Margins self-calibrate; absolute floors need
   remeasuring if the embedding model changes. All in config.
2. **Arbitration method rank assumes metadata > doc.** If an authoritative table ever
   lives INSIDE a doc collection, a weak metadata match elsewhere can outrank the true
   doc answer. Groundedness/title rungs soften; watch on new ingests.
3. **Tier 1.2b proportionality constants** (2-word multi-word columns, 5-char single)
   tuned to this schema's column-name lengths. Structural, but revisit on new schemas.
4. **Generic collection names** ('notes', 'files') would make name-token anchors and
   primacy fire on innocent words — naming discipline, or revisit Tier 1.2.

**Ultimate test agreed**: ingest a brand-new file and judge answers cold — no tuning pass.

Pending gate for this block: AG-03 + new NA-04 (fresh process) after primacy change;
then commit VOCAB-01 + guards.

## MODEL-01 + DATA-01 CLOSED (2026-07-10)

Root cause chain: Datatypes_FIX44 never had a model problem. parse_xml_rows
auto-detect ranked row tags by RAW FREQUENCY — 51 <Description> leaves beat 26
<Datatype> rows, every leaf "row" was empty, the LLM was never called, junk
schema correctly refused. Datatypes_FIX42 only worked by a tie broken in
document order. Fix: rank by YIELD (instances producing >=1 field: attribute
or text-bearing child) — the objective itself; ties keep document order;
all-zero-yield falls back to frequency. Confirmed: FIX44 -> Datatype (26/26),
32B schema {identifier: Name, type: BaseType, description: Description}.
MODEL-01: schema_model key was simply absent from nlp_config local_llm — set
to qwen3-vl-32b-instruct; "[SCHEMA LLM] using schema model" prints. UI config
Save rewrote local_llm wholesale (would delete undisplayed keys like
schema_model) — now merges. Gate: smoke_fix after re-ingest.

<<<<<<< Updated upstream
## SPEED-01 steps 1-3 DONE (2026-07-11) — remote-first network cuts

Context: user away from the NAS for ~a month; slow link is the NORMAL case.
Baseline #5 (eval_run_20260710_235752): quality best yet (~48/50 pending
verdicts — AG-07/09/10 all exact, MI-03 fixed, NA honest), but chat avg 137s
with 644s/1013s outliers — link RTT multiplication + possible 32B residency.

1. Stage timers in chat_turn (TIMER lines, DEBUG-gated): intent /
   contextualize / multi_item_gate / routing / retrieval / synthesis.
2. correct_words batched: ONE unnest tsvector call + ONE word=ANY membership
   (was up to 3 RTT per word); trigram only for unknowns; per-word path kept
   as fallback. Parity: 180 combos, 0 mismatches (diag_vocab_parity.py).
   Visible now: per-collection corrections are sloppy for short words
   ('prime'->'price', 'rerun'->'run') — PRE-EXISTING, parity-proven. Candidate
   follow-up: length-aware min_sim (<=5 chars needs ~0.85). User to decide.
3. Tier 1.25 batched (one query per word across collections, seating order
   preserved — diag_tier13 byte-identical) + in-process schema cache (60s TTL,
   invalidated on save/delete) consumed by Tier 1.2b and metadata_query.
   Smokes recon+fix green.

Step 4 (NOT started, config-gated, requires full eval): single shared intent
call, contextualize+intent merge, fast_model (llama-3.2-3b) for
rewrite/routing, streaming. PP-01-class cascades need their own look.
=======
## SPEED-01 step 4 DONE (2026-07-11) — chat avg 137s -> 26.6s

Eval eval_run_20260711_032930: **Chat avg 26.6s / p95 47.8s / max 50.1s**
(was 137s / 644s / 1013s); Ask 14.5s. Quality held — 18 of 20 answer diffs
were synthesis phrasing, DL-05 Chat IMPROVED (leads with the record now).

1. llm_detect_intent memoized per question (lock + capped cache; fallback
   results never cached) — the 3 byte-identical per-collection calls -> 1.
2. front_of_pipe(): ONE fast-model call replaces detect_chat_intent +
   contextualize_query; deterministic short-circuits preserved; full fallback
   to the two-call path on any failure. Config: local_llm.fast_model
   (llama-3.2-3b-instruct); clearing the key reverts to default model.
3. Config tab now edits schema_model + fast_model (empty = remove key).

Incidents during rollout (both fixed):
- The 3B echoed 'recon' as 'recent' in its "unchanged" standalone_query —
  routing corrupted. Guard: is_followup=false forces standalone_query =
  original question IN CODE. Lesson: never ask a model to copy what code can.
- Setting fast_model ACTIVATED a dormant _fast_model() hook in
  split_multi_item_question — the 3B silently answered is_multi=false and
  MI-02/MI-03 lost their splits (fail-safe = silent no-split). Splitter
  pinned to the default model with comment. Lesson: adding a config key can
  change behavior wherever that key was already consumed — grep before set.
- LM Studio "Only Keep Last JIT Loaded Model" was ON -> 14B/3B ping-pong
  (35s front calls). Toggle OFF; 32GB holds 14B+3B+bge comfortably. 32B must
  stay unloaded during chat.

Multi-item questions pay ~+15s for the 14B split call (gate-passers only;
everything else +0.0s). Remaining latency lives in per-collection spec
extraction (~7s x 3, serialized) — future candidate, judgment-quality risk.
SPEED-01 CLOSED for now: 5.2x avg, 20x max improvement, quality held.
>>>>>>> Stashed changes
