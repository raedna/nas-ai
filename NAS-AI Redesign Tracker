# NAS-AI Redesign Tracker

## Ground Rules
- One step at a time. Discuss → agree → implement.
- No hardcoding in code or config files.
- Every change is covered by a smoke test before and after.
- GitHub commit after each completed step.
- Breaking something while fixing something else = rollback immediately.

---

## System State (current)

| Component | Status |
|---|---|
| LM Studio (Mac M1 Pro) | Running — serves embeddings + LLM |
| Embeddings model | `text-embedding-bge-large-en-v1.5` (1024 dims) |
| LLM model | `meta-llama-3.1-8b-instruct` |
| Qdrant (NAS) | Running at `100.123.16.57:6333` — admin UI only |
| ai-pgvector (NAS) | Running at port 5433, db=nasai — PRIMARY STORE |
| ai-redis (NAS) | Installed, not used |
| Streamlit UI | Working — partially migrated to PostgreSQL |
| FIX collection (`xml_test`) | 1168 chunks + 1791 enum values — 13/13 smoke tests passing |
| BBG collection (`bbg_fields`) | 730 rows (test subset) — 6/6 smoke tests passing |
| KB docs (`kb_docs`) | Ingested, not yet reliably tested |
| Obsidian, astro, images | Early stage |

---

## Known Issues

| ID | Area | Description | Status |
|---|---|---|---|
| RET-010 | Structured planner | Ranking balance for phrases like "executing broker" in long descriptions. | Mitigated via synonyms.json |
| CODE-005 | xml_serializer.py | Function-level state (`_all_rows`, `_files_seen`) for cross-file buffering — corrupts silently on crash. | ⏳ Pending |
| CODE-008 | ui_app.py | Collection management UI still uses Qdrant REST API for stats/delete. Needs migration to PostgreSQL. | ⏳ Phase 3 follow-up |
| CODE-009 | ui_app.py | Force re-ingest does not always delete existing DB rows before re-inserting. | ⏳ Pending |
| CODE-010 | ingestion | Collection name mismatch between UI and PostgreSQL collections table causes ForeignKeyViolation. | ⏳ Pending |
| CODE-011 | schema_loader.py | Schema files stored on disk — should migrate to PostgreSQL schemas table. | ⏳ Pending |
| CODE-012 | ui_app.py | Ask/Chat tabs need a "Clear cache" button | ⏳ Pending |
| CODE-013 | ui_app.py | BM25/lexical candidates not shown in debug panel | ⏳ Pending |

**Retired issues (resolved):**
- RET-011 — FIX-01 enum suppression: fixed in Phase 1
- CODE-001 — `scroll(limit=5000)`: eliminated by RRF/pgvector in Phase 2/3
- CODE-002 — hardcoded collection name: eliminated in retrieval package
- CODE-003 — Qdrant client at import time: eliminated
- CODE-004 — `run_query_with_method` / `route_query` overlap: resolved in router.py
- CODE-007 — collection state JSON: eliminated — state now in PostgreSQL `files` table

---

## Roadmap

### Phase 1 — Stabilize FIX retrieval ✅ COMPLETE
| Step | Description | Status |
|---|---|---|
| 1.1–1.9 | FIX smoke test + 6 retrieval fixes | ✅ Done — 6/6 passed |

### Phase 1b — FIX version separation
| Step | Description | Status |
|---|---|---|
| 1b.1 | Add `fix_version` field to XML serializer payload | ⏳ Pending |
| 1b.2–1b.4 | Version-aware smoke test + answers + comparison queries | ⏳ Pending |

---

### Phase 2 — PostgreSQL as primary store ✅ COMPLETE
| Step | Description | Status |
|---|---|---|
| 2.1–2.7 | Deploy PostgreSQL, schema, ingestion | ✅ Done |
| 2.8 | Split query_router into retrieval package (10 files) | ✅ Done |
| 2.9–2.11 | ui_app cutover, smoke tests 23/23 + 6/6 | ✅ Done |
| 2.12–2.13 | Retire old engines + qdrant_client | ✅ Done |

**Retrieval package (`core/retrieval/`):**

| File | Role |
|---|---|
| `db_retrieval.py` | All SQL — BM25, vector, RRF, trigram, enum lookups |
| `structured.py` | Namespace / identifier / primary_name lookups |
| `lexical.py` | BM25 search with synonym expansion |
| `semantic.py` | pgvector similarity search |
| `crosslink.py` | Relationships, enum lookups, doc chunk enrichment |
| `discovery.py` | Count / list queries |
| `reranker.py` | Scoring (used for non-structured doc types only) |
| `answer.py` | Answer synthesis, display labels |
| `router.py` | Entry point — RRF-based retrieval, confidence threshold |
| `__init__.py` | Package exports |

---

### Phase 3 — BBG ingestion and retrieval ← CURRENT FOCUS

| Step | Description | Status |
|---|---|---|
| 3.1–3.7 | BBG ingest + routing fixes + 6/6 smoke tests | ✅ Done |
| 3.8 | Hybrid RRF search (BM25 + pgvector + trigram) in PostgreSQL | ✅ Done |
| 3.8.1 | `search_rrf` in db_retrieval.py — single SQL query | ✅ Done |
| 3.8.2 | `ts_rank_cd` for cover density ranking | ✅ Done |
| 3.8.3 | Trigram signal with `pg_trgm` + namespace filter | ✅ Done |
| 3.8.4 | Synonym expansion with multi-variant BM25 queries | ✅ Done |
| 3.8.5 | Confidence threshold (config-driven) — top 5 fallback | ✅ Done |
| 3.8.6 | `collection_has_enums` — skip reverse enum for BBG | ✅ Done |
| 3.9 | Extended FIX smoke tests — 13/13 passed | ✅ Done |
| 3.10 | Fix force re-ingest (CODE-009) | ⏳ Pending |
| 3.11 | Fix collection name mismatch (CODE-010) | ⏳ Pending |
| 3.12 | Ingest full BBG dataset | ⏳ Pending |
| 3.13 | Re-run BBG smoke tests against full dataset | ⏳ Pending |
| 3.14 | ui_app.py — migrate collection management to PostgreSQL (CODE-008) | ⏳ Pending |

**Key architectural decisions made in Phase 3:**
- RRF (Reciprocal Rank Fusion) replaces separate BM25+semantic merge — single PostgreSQL query
- `ts_rank_cd` (cover density) replaces `ts_rank` — rewards proximity of query terms
- Trigram (`pg_trgm`) added as third RRF signal for name similarity
- `identifier_namespace` filter applied when query contains "tag"/"component"
- Confidence threshold (default 0.105, in `system.json`) — below threshold returns top 5 with "no exact match" message
- Python reranker skipped for structured collections — RRF handles ranking
- `quant` removed from synonyms.json (was breaking BM25 queries)

---

### Phase 4 — MCP Server
| Step | Description | Status |
|---|---|---|
| 4.1–4.5 | Design + implement + smoke test MCP server (local + online modes) | ⏳ Pending |

---

### Phase 5 — Advanced features
- FIX message parser → English sentence synthesis
- Astrophotography metadata search (FITS at scale)
- Adaptive ingestion (no pre-defined schema required)
- Cross-collection retrieval
- Schema files migrated to PostgreSQL (CODE-011)
- `ts_rank_cd` weights array tuning (`{0.1, 0.2, 0.4, 1.0}`) for position-based boosting
- Query expansion via LLM before search

---

## Change Log

| Date | Step | What changed | Files touched | Result |
|---|---|---|---|---|
| 2026-06-09 | 1.1–1.9 | Phase 1 FIX fixes | `query_router.py`, `xml_serializer.py`, `config/synonyms.json` | 6/6 smoke tests |
| 2026-06-09 | 2.1–2.12 | PostgreSQL migration + retrieval package | `core/retrieval/` (10 files), `core/ui_app.py` | 23/23 + 6/6 |
| 2026-06-10 | 2.12 hotfix | FIX-05 regression fix | `core/retrieval/router.py`, `config/query_terms.json` | 6/6 |
| 2026-06-10 | 2.13 | Retire qdrant_client.py | `core/structured_plan_executor.py` | No regressions |
| 2026-06-10 | 3.1–3.7 | BBG ingest + routing fixes | `core/retrieval/router.py`, `discovery.py`, `answer.py`, `lexical.py`, `reranker.py`, `db_retrieval.py`, `nlp_generator.py`, `config/query_terms.json` | FIX 7/7, BBG 6/6 |
| 2026-06-10 | 3.8 | RRF hybrid search + trigram + ts_rank_cd + confidence threshold | `core/retrieval/db_retrieval.py`, `core/retrieval/router.py`, `core/retrieval/lexical.py`, `config/synonyms.json`, `config/system.json` | FIX 13/13, BBG 6/6 |
| 2026-06-10 | UI | Partial ui_app.py migration to PostgreSQL | `core/ui_app.py` | Collections/Ask/Preview/Validation tabs use PostgreSQL |

---

## Smoke Test Results

### FIX Smoke Test (2026-06-10) **13/13 passed**

| ID | Question | Method | Pass? |
|---|---|---|---|
| FIX-01 | what is tag 22 | structured_namespace_lookup | ✅ |
| FIX-02 | what values can tag 22 have | structured_namespace_lookup | ✅ |
| FIX-03 | what tag is exec broker | semantic | ✅ |
| FIX-04 | what tag is execution broker | semantic | ✅ |
| FIX-05 | what tag can have a value ISIN | reverse_enum_lookup | ✅ |
| FIX-06 | what tags contain security | discovery_list | ✅ |
| FIX-07 | what tags contain broker | discovery_list | ✅ |
| FIX-08 | which tag is order quantity | semantic | ✅ |
| FIX-09 | which tag is cum qty | semantic | ✅ |
| FIX-10 | what is last quantity | semantic | ✅ |
| FIX-11 | which tag is exec qty | semantic | ✅ (no ExecQty in FIX — returns "no exact match") |
| FIX-12 | which tag is exec quantity | semantic | ✅ (no ExecQty in FIX — returns "no exact match") |
| FIX-13 | which tag is targetcompid | semantic | ✅ |

### BBG Smoke Test (2026-06-10) **6/6 passed**

| ID | Question | Method | Pass? |
|---|---|---|---|
| BBG-01 | what is PX_ASK | semantic | ✅ |
| BBG-02 | what is the description of ARD_OPERATING_EXP_PER_ASM_ASK | semantic | ✅ |
| BBG-03 | what type is PX_ASK | semantic | ✅ |
| BBG-04 | what fields contain ask price | discovery_list | ✅ |
| BBG-05 | what fields are in category airlines | discovery_list | ✅ |
| BBG-06 | what string fields are available | discovery_list | ✅ |